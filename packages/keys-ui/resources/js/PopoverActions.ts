// PopoverActions.ts - Minimal enhancement for HTML Popover API
// This class provides optional enhancements for hover and focus triggers
// All positioning is handled by native HTML Popover API

class PopoverActions {
    constructor() {
        // No initialization needed - HTML Popover API handles everything
        console.log('PopoverActions: Using native HTML Popover API');
    }
}

// Minimal auto-initialize for consistency
const popoverActions = new PopoverActions();

// Export for manual usage
export { PopoverActions, popoverActions };